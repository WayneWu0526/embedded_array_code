# Local Data Staging

This directory is for temporary local experiment data only.

Large recordings, generated reports, and analysis outputs should usually go to
`~/mi_gels_data`, NAS, or another external archive. Git ignores data files here
by default while keeping this README and `.gitkeep` files trackable.
