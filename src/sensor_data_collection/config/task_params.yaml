# 通用任务参数
mode: "CVT"              # 当前模式: CVT 或 CCI (切换模式时修改此处即可)
num_cycles: 30

# Slot -> TF frame 映射
# key 为 slot 编号（字符串），value 为 frame 名称，"null" 表示该 slot 无 pose
slot_frame_mapping:
  "0": "diana7_em_tcp_filt"
  "1": "arm1_em_tcp_filt"
  "2": "arm2_em_tcp_filt"
  "3": "null"
